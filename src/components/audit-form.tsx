"use client";
import{FormEvent,useState}from"react";import type{AuditResult}from"@/lib/types";import{LoadingState}from"./loading-state";import{AuditReport}from"./audit-report";
export function AuditForm(){const[url,setUrl]=useState("");const[state,setState]=useState<"idle"|"loading"|"error"|"done">("idle");const[error,setError]=useState("");const[result,setResult]=useState<AuditResult|null>(null);
async function submit(e:FormEvent){e.preventDefault();if(!url.trim()){setError("Please enter a website URL.");setState("error");return}setState("loading");setError("");try{const res=await fetch("/api/audit",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({url})});const data=await res.json();if(!res.ok||!data.success)throw new Error(data.error||"We couldn't analyze this website.");setResult(data);setState("done")}catch(e){setError(e instanceof Error?e.message:"Something unexpected went wrong.");setState("error")}}
function reset(){setState("idle");setResult(null);setError("");window.scrollTo({top:0,behavior:"smooth"})}
return <><form className="audit-form" onSubmit={submit}><div><label className="sr-only" htmlFor="url">Website URL</label><input id="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com" inputMode="url" autoComplete="url"/><button disabled={state==="loading"}>{state==="loading"?"Auditing…":"Audit Website →"}</button></div>{state==="error"&&<p className="form-error" role="alert">⚠️ {error}</p>}<p className="free">Free website health check — performance, accessibility, best practices &amp; SEO</p></form>{state==="loading"&&<LoadingState/>}{state==="done"&&result&&<AuditReport result={result} onReset={reset}/>}</>}
