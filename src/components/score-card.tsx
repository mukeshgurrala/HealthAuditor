export function ScoreCard({label,score,featured=false}:{label:string;score:number|null;featured?:boolean}){const value=score??0;const color=value>=90?"#10b981":value>=50?"#f59e0b":"#f43f5e";return <article className={`score-card ${featured?"featured":""}`}><div className="ring" style={{background:`conic-gradient(${color} ${value*3.6}deg,#ede9fe 0)`}}><div><strong style={{color}}>{score??"—"}</strong><small>/100</small></div></div><h3>{label}</h3></article>}
